Hadoop-Projekte (Use Cases + Quellcode verstehen):
  # Erstellung von Use Cases 
  # Klärung von Source-Code auf Basis von Funktionalität-Workflow.
      - Definitive Guide
      - Was passiert nach Ausführen vom jeden Command, z.B. sbin/start-dfs.sh
      - Internet (Stackoverflow)
  # Hadoop Operations Framework
  # ELK 
      - Logging-Analyse
      - Integration mit Hadoop


In der Restzeit vor allem Verständnis über Quellcode schaffen.


    # Generelle Architektur-Optimierung (Hadoop)
    # Unternehmensweite Doku-Pool (SearchEngine gebraucht, ELK, Solr etc.)
    # Visualisierungstool (Qlikview)
        - Use Cases (soweit vorhandene Beispiele)
        - Verständnisse über die Source-Code-Funktionalität hinter den Use-Cases
        


Wie debuggen?
  # Check logfile, check data-repository


Manche (Prozent?) Daten fließen nach Host
Manche (Prozent?) Daten fließen nach SAP
Inbound-Daten aus FTP, Webservice z.B.

Konkretere Anwendungsfälle z.B. auf Basis von Oozie zu Hadoop.
Gibt's Bedarf nach Storm?
Zuerst ElasticSearch


Da IT-Abteilung stark vom Business ableitend ist, daher Motivation nach einer IT-Produkt-Driven-Unternehmen

Vergleichbar:
  # Sqoop u. Axway Pickup

Vergleich zwischen Oozie und cron
  # https://stackoverflow.com/questions/29647978/which-is-the-best-scheduler-for-hadoop-oozie-or-cron
  # http://beekeeperdata.com/posts/hadoop/2016/07/19/Cron-Alternatives-For-Hadoop.html

#################################################################

Hadoop Erfahrung:
  # erste Erfahrung gesammlt, weil Firma neue Daten von Container-Überwachung aufnehmen, und neue Architektur (Hadoop) gebraucht ist.
  # Daher erste Vorbereitung für die potentielle Hadoop-Projekte
  # 

ML-Erfahrung vor allem in Uni:
  # 


##############################################

CV
  Ausbildung
  Hadoop-Projekt-Entwicklungserfahrung
    # Links (in Zhihu) dabei
  Berufslauf
  Weiterbildung

Interview-Text:
  Subgliederungen, damit bessere strukturiertes Reden


Arbeit:
  Hackerrank
  Verständnis von Bücher-Beispielen
  Java-Beispiele
  Design-Pattern
  ElasticSearch, Logstash, Kibana
