Manche (Prozent?) Daten fließen nach Host
Manche (Prozent?) Daten fließen nach SAP
Inbound-Daten aus FTP, Webservice z.B.

Konkretere Anwendungsfälle z.B. auf Basis von Oozie zu Hadoop.
Gibt's Bedarf nach Storm?
Zuerst ElasticSearch

Mögliche Projekte:
  # Generelle Architektur-Optimierung (Hadoop)
  # Unternehmensweite Doku-Pool (SearchEngine gebraucht, ELK, Solr etc.)
  # Visualisierungstool (Qlikview)

Da IT-Abteilung stark vom Business ableitend ist, daher Motivation nach einer IT-Produkt-Driven-Unternehmen



#################################################################

Hadoop Erfahrung:
  # erste Erfahrung gesammlt, weil Firma neue Daten von Container-Überwachung aufnehmen, und neue Architektur (Hadoop) gebraucht ist.
  # Daher erste Vorbereitung für die potentielle Hadoop-Projekte
  # 

ML-Erfahrung vor allem in Uni:
  # 


##############################################

CV
  Ausbildung
  Hadoop-Projekt-Entwicklungserfahrung
    # Links (in Zhihu) dabei
  Berufslauf
  Weiterbildung

Interview-Text:
  Subgliederungen, damit bessere strukturiertes Reden


Arbeit:
  Hackerrank
  Verständnis von Bücher-Beispielen
  Java-Beispiele
  Design-Pattern
  ElasticSearch, Logstash, Kibana
